---
name: Bug report
about: Something behaves differently from what the docs describe
title: ""
labels: bug
assignees: ""
---

<!-- Security problems: do NOT file them here — see SECURITY.md → "Reporting a vulnerability". -->

**What happened**

**What you expected**

**How to reproduce**

<!-- Ideally a small directory of dummy files (`touch ...`) and the exact command. -->

**Environment**

- organizer version (`organizer --version`):
- How installed (`.deb`, `install.sh`, checkout):
- Distribution and kernel (`uname -r`):
- Python (`/usr/bin/python3 -V`):
- `organizer status` output (shows daemon, memory and sandbox state):

**Report / logs**

<!-- If relevant: the JSON report (`organizer <dir> --json`, file names redacted as needed)
     and `journalctl --user -u organizer -n 50`. -->
